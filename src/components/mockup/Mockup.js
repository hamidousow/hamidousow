import React from 'react';

const Mockup = (props) => {
    return (
        <div className="containerMockup">
            {props.children}            
        </div>
    );
};

export default Mockup;